    lda #$68
    sta $FFFB

    lda #$65
    sta $FFFB

    lda #$6c
    sta $FFFB

    lda #$6c
    sta $FFFB

    lda #$6f
    sta $FFFB

    lda #$20
    sta $FFFB

    lda #$77
    sta $FFFB

    lda #$6f
    sta $FFFB

    lda #$72
    sta $FFFB

    lda #$6c
    sta $FFFB

    lda #$64
    sta $FFFB
